[package]
name = "superstruct"
version = "0.1.0"
edition = "2021"
description = "Adaptive in-memory poly-index data structure"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
md-5 = "0.10"
regex = "1"
rand = "0.8"

[dev-dependencies]
tempfile = "3"
